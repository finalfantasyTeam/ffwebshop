@model WebShop.Web.Models.ProductCategoryViewModel
@using (Html.BeginForm("Create", "AdminProductCategory", FormMethod.Post))
{
<div class="row">
    <div class="col-md-6">
        <div class="box box-primary">
            <div class="box-header with-border">
              @if (@Model.ProductCategory != null)
              {
                  <h3 class="box-title">@Model.ProductCategory.Id - @Model.ProductCategory.Name</h3>
              }
              else
              {
                  <h3 class="box-title">Product Category name</h3>
              }
              <a class="btn-link">@L("BackButtonLabel")</a>
            </div>
            <form role="form">
                <div class="box-body">
                    <div class="form-group">
                        <label for="txtCategoryName">@L("NameLabel")</label>
                        @Html.TextBoxFor(@model => @model.ProductCategory.Name, new { @id = "txtCategoryName", @class = "form-control" })
                    </div>
                    <div class="form-group">
                      <label for="txtCategoryDescription">@L("DescriptionLabel")</label>
                      @Html.TextBoxFor(@model => @model.ProductCategory.Name, new { @id = "txtCategoryName", @class = "form-control" })
                   </div>
                   <div class="form-group">
                      <label for="fileCategoryLogo">@L("ImageToShowLabel")</label>
                      <input type="file" name="File" />
                      <p class="help-block">@L("UploadLogoHelpText")</p>
                      @*@Model.ProductCategory.ImageToShow*@
                   </div>
                  <div class="checkbox">
                    <label>
                      @Html.CheckBoxFor(@model => @model.ProductCategory.IsActive) @L("IsActiveLabel")
                    </label>
                  </div>
                </div>
                <div class="box-footer">
                    <button type="button" class="btn btn-primary">@L("SaveButtonLabel")</button>
                </div>
            </form>
        </div>
    </div>
</div>
}